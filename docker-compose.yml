services:
  wingjobs:
    build: .
    expose:
      - "8000"             # visible uniquement sur le réseau Docker, pas sur le net
    volumes:
      - wingjobs-data:/app/data
    environment:
      - DB_FILE=/app/data/wingjobs.db
    env_file:
      - .env
    networks:
      - web
    restart: unless-stopped

volumes:
  wingjobs-data:

networks:
  web:
    external: true         # réseau partagé avec caddy-cv
